package br.com.fuctura.projeto.enuns;

public enum Estatura {

    // por padrao, todo enum se escreve com letra maiúscula

    BAIXA(0),
    MEDIA(1),
    ALTA(2);

    Estatura(Integer cod) {
    }
}
